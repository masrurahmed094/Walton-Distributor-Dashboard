import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss']
})
export class AppComponent {
  // The title property is a standard part of the root component,
  // though it is not displayed directly in our template.
  title = 'distributor-dashboard-frontend';
}